DuGS 
DuGS vision is for it to be a node builder like n8n and also for Robotics. 
But one that is esaly hostabale from most if not any device.
Its currenty in devlopment mostly build with AI.

REQUIRMENTS:
Python 3.9 or higher
+
sudo apt install python3-pip
pip install --break-system-packages PyQt6
sudo apt install libxcb-cursor0 libxcb-icccm4

COMMANDS:
To Start open the terminala for linux you use this comand structure
cd "PATH" python3 api.py

Open another Termnal and use this command structure
cd "PATH" GDK_BACKEND=x11 python3 ui.py
